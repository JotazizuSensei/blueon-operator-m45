const VERSION='eurolab-v5.0.0';
const CACHE=`${VERSION}-static`;
const STATIC=['index.html','v5.css','app-v5.js','manifest.webmanifest','icon-192.png','icon-512.png'];
self.addEventListener('install',event=>{self.skipWaiting();event.waitUntil(caches.open(CACHE).then(async cache=>{for(const asset of STATIC){try{const r=await fetch(`${asset}?v=${VERSION}`,{cache:'no-store'});if(r.ok)await cache.put(asset,r.clone())}catch{}}}))});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(url.origin!==self.location.origin)return;if(event.request.mode==='navigate'){event.respondWith(fetch(event.request,{cache:'no-store'}).then(async r=>{if(r.ok){const c=await caches.open(CACHE);await c.put('index.html',r.clone())}return r}).catch(()=>caches.match('index.html')));return}if(url.pathname.endsWith('/latest-results.json')||url.pathname.endsWith('/latest-result.json')){event.respondWith(fetch(event.request,{cache:'no-store'}).catch(()=>caches.match(event.request)));return}const name=url.pathname.split('/').pop();if(STATIC.includes(name)){event.respondWith(caches.match(name).then(cached=>cached||fetch(event.request).then(async r=>{if(r.ok){const c=await caches.open(CACHE);await c.put(name,r.clone())}return r})));return}event.respondWith(fetch(event.request).catch(()=>caches.match(event.request)))});