seModal()">Cancelar</button><button class="btn primary" onclick="app.saveEditTask('${id}')">Guardar</button></div>`);},
 saveEditTask(id){const t=state.tasks.find(x=>x.id===id);t.title=document.getElementById('etitle').value.trim()||t.title;t.due=document.getElementById('edue').value||t.due;saveLocal();closeModal();renderAll();},
 newOpp(pref={}){showModal(`<h2>Nova oportunidade</h2><div class="field"><label>Título</label><input id="otitle" value="${esc(pref.title||'')}"></div><div class="fields" style="margin-top:9px"><div class="field"><label>Área</label><select id="odomain"><option>BlueON</option><option>Blue Fitness</option><option>Blue Pilates</option><option>3D Lab</option><option>AI Commerce</option><option>Conteúdo</option><option>Capital</option><option>Outro</option></select></div><div class="field"><label>Score 0–100</label><input id="oscore" type="number" min="0" max="100" value="${pref.score||70}"></div><div class="field"><label>Próximo teste</label><input id="onext" value="${esc(pref.next||'Validar procura / pagar')}"></div></div><div class="modalactions"><button class="btn" onclick="closeModal()">Cancelar</button><button class="btn primary" onclick="app.saveOpp()">Guardar</button></div>`);},
 saveOpp(){const title=document.getElementById('otitle').value.trim();if(!title)return;const o={id:uid(),title,domain:document.getElementById('odomain').value,score:Number(document.getElementById('oscore').value||0),next:document.getElementById('onext').value,status:'inbox',createdAt:new Date().toISOString()};state.opportunities.push(o);state.tasks.push({id:uid(),projectId:domainProject(o.domain),title:`Validar: ${o.title}`,due:addDays(2),status:'todo',kind:'opportunity',opportunityId:o.id});activity(`Oportunidade criada: ${o.title}`,'opportunity');saveLocal();closeModal();renderAll();},
 advanceOpp(id){const o=state.opportunities.find(x=>x.id===id);if(!o)return;const flow=['inbox','testing','building','live'];const i=flow.indexOf(o.status);o.status=flow[Math.min(i+1,flow.length-1)];if(o.status==='testing')state.tasks.push({id:uid(),projectId:domainProject(o.domain),title:`Teste mínimo: ${o.title}`,due:addDays(5),status:'todo',kind:'opportunity',opportunityId:o.id});if(o.status==='live')this.createDistributionForOpportunity(o);activity(`Oportunidade ${o.status}: ${o.title}`,'opportunity');saveLocal();renderAll();},
 killOpp(id){const o=state.opportunities.find(x=>x.id===id);if(o)o.status='kill';saveLocal();renderAll();},
 radarToOpp(id){const r=findRadar(id);if(r)this.newOpp({title:r.title,score:r.score,next:'Confirmar procura, monetização e vantagem'});},
 radarToContent(id){const r=findRadar(id);if(!r)return;state.content.push(makeContent(r.title,goalFromCategory(r.category),formatFromCategory(r.category)));activity(`Radar virou conteúdo: ${r.title}`,'content');saveLocal();renderAll();toast('Pacote de conteúdo criado.');},
 ignoreRadar(id){if(!state.radarIgnored.includes(id))state.radarIgnored.push(id);saveLocal();renderAll();},
 async refreshRadar(){if(!syncCreds){toast('Liga os dispositivos/cloud para atualizar o Radar.');this.auth();return}setSync('syncing','Radar...');try{const data=await hub('radar');state.radar=data.items||[];state.radarUpdatedAt=data.updatedAt||new Date().toISOString();activity('Radar atualizado','radar');saveLocal();renderAll();setSync('ok','Cloud ligada');toast('Radar atualizado.')}catch(e){console.error(e);setSync('error','Erro Radar');toast('Não consegui atualizar o Radar.');}},
 newContent(){showModal(`<h2>Novo pacote de conteúdo</h2><div class="field"><label>Tema / ativo principal</label><input id="ctitle"></div><div class="fields" style="margin-top:9px"><div class="field"><label>Objetivo</label><select id="cgoal"><option>Vender</option><option>Gerar leads</option><option>Educar</option><option>Awareness</option><option>Prova social</option></select></div><div class="field"><label>Formato principal</label><select id="cformat"><option>YouTube</option><option>Reel</option><option>Carrossel</option><option>Story</option><option>Master/Aula</option></select></div><div class="field"><label>Área</label><select id="cproject">${state.projects.map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('')}</select></div></div><div class="modalactions"><button class="btn" onclick="closeModal()">Cancelar</button><button class="btn primary" onclick="app.saveContent()">Criar 1→muitos</button></div>`);},
 saveContent(){const title=document.getElementById('ctitle').value.trim();if(!title)return;const c=makeContent(title,document.getElementById('cgoal').value,document.getElementById('cformat').value,document.getElementById('cproject').value);state.content.push(c);this.scheduleContentTasks(c);activity(`Pacote criado: ${title}`,'content');saveLocal();closeModal();renderAll();},
 scheduleContent(id){const c=state.content.find(x=>x.id===id);if(c){this.scheduleContentTasks(c);saveLocal();renderAll();toast('Conteúdo calendarizado.');}},
 scheduleContentTasks(c){if(c.scheduled)return;c.scheduled=true;(c.derivatives||[]).slice(0,5).forEach((d,i)=>state.tasks.push({id:uid(),projectId:c.projectId||'creative',title:`Conteúdo · ${d}: ${c.title}`,due:addDays(i+1),status:'todo',kind:'content',contentId:c.id}));},
 new3D(){showModal(`<h2>Novo produto / design 3D</h2><div class="field"><label>Nome</label><input id="pname"></div><div class="field" style="margin-top:8px"><label>Problema real</label><textarea id="pproblem"></textarea></div><div class="fields" style="margin-top:9px"><div class="field"><label>Material €</label><input id="pmaterial" type="number" step=".01" value="1"></div><div class="field"><label>Horas máquina</label><input id="phours" type="number" step=".1" value="2"></div><div class="field"><label>Horas humanas</label><input id="phuman" type="number" step=".1" value="0.25"></div><div class="field"><label>Embalagem €</label><input id="ppack" type="number" step=".01" value="1"></div></div><label class="item" style="margin-top:8px"><input type="checkbox" id="pdigital"><div class="grow"><div class="title">Ativo digital</div><div class="meta">STL/3MF/STEP/licença em vez de produto físico.</div></div></label><div class="modalactions"><button class="btn" onclick="closeModal()">Cancelar</button><button class="btn primary" onclick="app.save3D()">Guardar</button></div>`);},
 save3D(){const name=document.getElementById('pname').value.trim();if(!name)return;const p={id:uid(),name,problem:document.getElementById('pproblem').value,material:Number(document.getElementById('pmaterial').value),hours:Number(document.getElementById('phours').value),human:Number(document.getElementById('phuman').value),pack:Number(document.getElementById('ppack').value),failPct:10,machineRate:1.2,laborRate:12,digital:document.getElementById('pdigital').checked,stage:'IDEA',createdAt:new Date().toISOString()};state.products3d.push(p);state.tasks.push({id:uid(),projectId:'lab3d',title:`Validar problema 3D: ${name}`,due:addDays(3),status:'todo',kind:'3d',productId:p.id});activity(`3D criado: ${name}`,'3d');saveLocal();closeModal();renderAll();},
 edit3D(id){const p=state.products3d.find(x=>x.id===id);if(!p)return;showModal(`<h2>${esc(p.name)}</h2><div class="fields"><div class="field"><label>Material €</label><input id="epmat" type="number" step=".01" value="${p.material}"></div><div class="field"><label>Horas máquina</label><input id="eph" type="number" step=".1" value="${p.hours}"></div><div class="field"