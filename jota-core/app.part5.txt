grid g2" style="margin-top:12px"><div class="card"><h3>Cloud</h3><p>${session?`Ligado como <b>${esc(session.user.email||'utilizador')}</b>.`:'Não ligado. Os dados estão apenas neste dispositivo.'}</p><button class="btn ${session?'':'primary'}" onclick="app.auth()">${session?'Gerir conta':'Ligar / criar conta'}</button>${session?'<button class="btn" style="margin-left:6px" onclick="app.logout()">Sair</button>':''}</div><div class="card"><h3>Radar automático</h3><label class="item"><input type="checkbox" ${state.settings.autoRadar?'checked':''} onchange="app.setting('autoRadar',this.checked)"><div class="grow"><div class="title">Atualizar ao abrir</div><div class="meta">Quando existe sessão cloud e passou o intervalo definido.</div></div></label><div class="field" style="margin-top:8px"><label>Intervalo (horas)</label><input type="number" min="1" max="48" value="${state.settings.radarHours||6}" onchange="app.setting('radarHours',this.value)"></div></div></div>`+
 `<div class="section"><h2>Backup</h2></div><div class="card"><div class="heroactions"><button class="btn" onclick="app.exportData()">Exportar JSON</button><button class="btn" onclick="document.getElementById('importFile').click()">Importar</button><input id="importFile" type="file" accept=".json" hidden onchange="app.importData(this.files[0])"><button class="btn ghost" onclick="app.reset()">Repor app</button></div></div>`+
 `<div class="section"><h2>O que é automático nesta versão</h2></div><div class="card"><p>✓ sincronização cloud entre dispositivos quando autenticado<br>✓ radar de notícias/oportunidades recentes ao abrir<br>✓ etapas → tarefas → calendário<br>✓ oportunidade → tarefas/ações<br>✓ conteúdo principal → derivados<br>✓ 3D → custos/preço/canais<br>✓ sugestões “próxima melhor ação”</p><p><b>Não faz automaticamente:</b> gastar dinheiro, publicar anúncios, executar trades ou publicar conteúdo externo sem aprovação.</p></div>`;
}

function nextBestActions(){
 const out=[];
 const blue=state.steps.blueon?.find(s=>s.status==='current');if(blue)out.push({id:'blue-step',area:'BlueON',title:blue.title,why:'É o motor com maior potencial de recorrência e a etapa atual está aberta.',level:'good'});
 const overdue=state.tasks.filter(t=>t.status!=='done'&&t.due<TODAY());if(overdue.length)out.push({id:'overdue',area:'Execução',title:`Fechar ${Math.min(overdue.length,3)} tarefa(s) atrasada(s)`,why:'Atraso acumulado cria dispersão. Fecha ou reagenda antes de abrir novos projetos.',level:'warn'});
 if(state.radar.length&&state.opportunities.length===0)out.push({id:'radar-op',area:'Radar',title:'Transformar 1 sinal recente em teste',why:'Há informação recente, mas ainda não existe oportunidade formal criada.'});
 if(state.content.length<2)out.push({id:'content-pack',area:'Conteúdo',title:'Criar 1 pacote 1→5',why:'Precisamos de transformar trabalho existente em distribuição reutilizável.',level:'purple'});
 if(state.products3d.length===0)out.push({id:'3d-first',area:'3D Lab',title:'Registar o primeiro problema real',why:'O 3D só começa quando existe uma fricção concreta, não com um STL aleatório.'});
 if(!state.radarUpdatedAt||Date.now()-Date.parse(state.radarUpdatedAt)>86400000)out.push({id:'refresh-radar',area:'Inteligência',title:'Atualizar radar',why:'O radar está vazio ou tem mais de 24 horas.'});
 return out.slice(0,6);
}

function euro(v){return new Intl.NumberFormat('pt-PT',{style:'currency',currency:'EUR'}).format(Number(v||0))}
function findRadar(id){return state.radar.find(x=>x.id===id)}

function showModal(html){document.getElementById('modal').innerHTML=html;document.getElementById('modalWrap').classList.add('show')}
function closeModal(){document.getElementById('modalWrap').classList.remove('show')}
document.getElementById('modalWrap').addEventListener('click',e=>{if(e.target.id==='modalWrap')closeModal()});

const app={
 go(v){activeView=v;document.querySelectorAll('.view').forEach(x=>x.classList.toggle('active',x.id===v));syncNav();window.scrollTo({top:0,behavior:'smooth'});},
 setActiveProject(pid){state.settings.activeProject=pid;saveLocal();renderAll();},
 toggleTask(id){const t=state.tasks.find(x=>x.id===id);if(!t)return;t.status=t.status==='done'?'todo':'done';activity(`${t.status==='done'?'Concluído':'Reaberto'}: ${t.title}`,'task');saveLocal();renderAll();},
 completeStep(pid,sid){const arr=state.steps[pid],idx=arr.findIndex(s=>s.id===sid);if(idx<0)return;arr[idx].status='done';state.tasks.filter(t=>t.sourceStep===sid).forEach(t=>t.status='done');if(arr[idx+1]){arr[idx+1].status='current';if(!state.tasks.some(t=>t.sourceStep===arr[idx+1].id))state.tasks.push({id:uid(),projectId:pid,title:arr[idx+1].title,due:arr[idx+1].due,status:'todo',sourceStep:arr[idx+1].id,kind:'step'});}activity(`Etapa concluída: ${arr[idx].title}`,'step');saveLocal();renderAll();toast('Próxima etapa desbloqueada.');},
 quickAdd(){showModal(`<h2>Criar</h2><div class="quick"><button onclick="app.newOpp();closeModal()"><b>Oportunidade</b><small>ideia/teste</small></button><button onclick="app.newTask();closeModal()"><b>Tarefa</b><small>calendarizar</small></button><button onclick="app.newContent();closeModal()"><b>Conteúdo</b><small>1→muitos</small></button><button onclick="app.new3D();closeModal()"><b>3D</b><small>produto/design</small></button></div><div class="modalactions"><button class="btn" onclick="closeModal()">Fechar</button></div>`);},
 newTask(){showModal(`<h2>Nova tarefa</h2><div class="fields"><div class="field"><label>Título</label><input id="mtitle"></div><div class="field"><label>Área</label><select id="mproj">${state.projects.map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('')}</select></div><div class="field"><label>Data</label><input id="mdue" type="date" value="${TODAY()}"></div></div><div class="modalactions"><button class="btn" onclick="closeModal()">Cancelar</button><button class="btn primary" onclick="app.saveTask()">Guardar</button></div>`);},
 saveTask(){const title=document.getElementById('mtitle').value.trim();if(!title)return;state.tasks.push({id:uid(),title,projectId:document.getElementById('mproj').value,due:document.getElementById('mdue').value||TODAY(),status:'todo',kind:'manual'});activity(`Tarefa criada: ${title}`,'task');saveLocal();closeModal();renderAll();},
 editTask(id){const t=state.tasks.find(x=>x.id===id);if(!t)return;showModal(`<h2>Editar tarefa</h2><div class="fields"><div class="field"><label>Título</label><input id="etitle" value="${esc(t.title)}"></div><div class="field"><label>Data</label><input id="edue" type="date" value="${t.due}"></div></div><div class="modalactions"><button class="btn" onclick="closeModal()">Cancelar</button><button class="btn primary" onclick="app.saveEditTask('${id}')">Guardar</button></div>`);},
 saveEditTask(id){const t=state.tasks.find(x=>x.id===id);t.title=document.getElementById('etitle').value.trim()||t.title;t.due=document.getElementById('edue').value||t.due;saveLocal();closeModal();renderAll();},
 newOpp(pref={}){showModal(`<h2>Nova oportunidade</h2><div class="field"><label>Título</label><input id="otitle" value="${esc(pref.title||'')}"></div><div class="fields" style="margin-top:9px"><div class="field"><label>Área</label><select id="odomain"><option>BlueON</option><option>Blue Fitness</option><option>Blue Pilates</option><option>3D Lab</option><option>AI Commerce</option><option>Conteúdo</option><option>Capital</option><option>Outro</option></select></div><div class="field"><label>Score 0–100</label><input id="oscore" t